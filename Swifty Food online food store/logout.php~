<?php
   setcookie("userId", "", time() - 3600);
   if(!isset($_COOKIE["userId"])){
   	header( 'Location: home.html' );
   }
  else{ 
	setcookie("userId", "", time() - 3600);
        header( 'Location: home.html' );
  }
?>
